<?xml version="1.0"?>
<package xmlns="http://schemas.microsoft.com/packaging/2013/05/nuspec.xsd">
  <metadata>
    <id>SharePointPackageProjectOutputAssemblies</id>
    <version>$version$</version>
    <authors>Patrick Westerhoff</authors>
    <owners>poke</owners>
    <requireLicenseAcceptance>false</requireLicenseAcceptance>
    <copyright>$copyright$</copyright>

    <projectUrl>https://github.com/poke/SharePointPackageProjectOutputAssemblies</projectUrl>
    <repository url="https://github.com/poke/SharePointPackageProjectOutputAssemblies" />

    <title>SharePoint: Package project output assemblies</title>
    <summary>MSBuild targets to support packaging other assemblies from the project output directory into the SharePoint solution by specifying the source path of custom assemblies using special replacement tokens.</summary>
    <description>This package contains MSBuild targets to support packaging other assemblies from the project output directory into the SharePoint solution package. It enables the use of special tokens in the source path for custom assemblies within the solution’s `Package.package` file. The supported tokens are `&#36;OutputPath&#36;` and `&#36;Configuration&#36;`, which will be replaced respectively by the build output path or the current build configuration.</description>
    <tags>SharePoint MSBuild</tags>

    <dependencies>
      <group targetFramework=".NETFramework4.6.1" />
    </dependencies>
  </metadata>
  <files>
    <file src="bin\$config$\net461\SharePointPackageProjectOutputAssemblies.dll" target="build\SharePointPackageProjectOutputAssemblies.dll" />
    <file src="SharePointPackageProjectOutputAssemblies.targets" target="build\SharePointPackageProjectOutputAssemblies.targets" />
  </files>
</package>
